# Risk Register

| id | severity | owner | risk | mitigation | status |
| --- | --- | --- | --- | --- | --- |
| R-001 | high | codex | Existing dirty worktree may overlap with implementation targets | Hard-synced workspace to `origin/master` before BATCH:009 and continue scoped-only edits | mitigated |
| R-002 | high | codex | QA bypass may leak into unintended runtime | Keep behavior per user requirement; enforce explicit flags and document QA-only intent | accepted_for_qa |
| R-003 | medium | codex | WebSocket auth claim mismatch breaks targeted delivery | Aligned WS auth extraction to JWT `sub` via `tokensService.verifyToken` | mitigated |
| R-004 | medium | codex | Mixed token key usage (`token` vs `auth_token`) causes inconsistent client auth | Normalized to `STORAGE_KEYS.AUTH_TOKEN` in API/middleware/dashboard paths | mitigated |
| R-005 | low | codex | Tailwind ambiguous utility warnings appear during frontend build output | Replaced ambiguous `ease-[cubic-bezier(...)]` tokens with stable CSS utility classes in BATCH:011 | mitigated |
| R-006 | medium | codex | Architecture convergence touched multiple domains in one batch | Explicitly marked as cross-domain in STATUS and documented ownership/decisions | mitigated |
| R-007 | medium | codex | Frontend now consumes shared contracts from server source tree (`@contracts/*`) and could break if server contract path moves | Documented in decisions + path aliases; future path move must include alias migration and contract import audit | mitigated |
| R-008 | low | codex | Notification websocket payload remains permissive (`Record<string, unknown>`), requiring narrow cast in UI page | Keep runtime behavior unchanged for QA; add typed notification payload map in Phase 8 error/resilience or Phase 9 tests | open |
| R-009 | high | codex | Dev login exposure can remain broad during QA if left default-enabled in non-test environments | Keep as accepted QA behavior per explicit user instruction; exposure still deterministic via `runtime.enableDevRoutes` and removable later by config flip | accepted_for_qa |
| R-010 | high | codex | `runtimeFlags` imports were committed without guaranteed committed source/export in remote history | Added `src/config/runtime.flags.ts` and `src/config/index.ts` re-export to Phase 3 push scope; re-verified build/lint/typecheck | mitigated |
| R-011 | low | codex | Tailwind CLI warnings (ambiguous arbitrary utility tokens) are not captured by ESLint fail-on-warning gate | Cleared known warnings in BATCH:011 and re-verified clean `npm run build` output | mitigated |
| R-014 | low | codex | Additional manual chunk splitting can increase request count on first loads for animation-heavy routes | Scoped chunk split to heavy libraries only and validated full build/quality gates; monitor route-level perf in Phase 6 evidence pass | monitoring |
| R-015 | low | codex | Bundle budget patterns can become stale as chunk naming/ownership evolves and may fail unexpectedly in future refactors | Added explicit budget policy + deterministic checker in BATCH:012; keep thresholds/patterns versioned in docs/execution and update with each intentional chunk strategy change | monitoring |
| R-012 | high | codex | Previous commit left CI-critical fixes only in local workspace (not committed), causing production build/lint failures on master | Shipped BATCH:008 hotfix commit with only failing files + full quality gate verification before push | mitigated |
| R-013 | high | codex | Execution docs drifted from committed route architecture artifacts, causing handoff ambiguity | BATCH:009 corrected STATUS/DECISIONS and explicitly documented deferred artifacts; future batches must validate docs against `HEAD` before close | mitigated |
| R-016 | medium | codex | Responsiveness verification could miss regressions if only manual spot-checking is used | Implemented deterministic Playwright-based full route/breakpoint audit and stored screenshot/report evidence in `docs/execution/evidence/BATCH_013_RESPONSIVENESS` | mitigated |
| R-017 | low | codex | Responsive patches might unintentionally alter QA auth or onboarding behavior | Confined Phase 6 edits to layout/touch-target/styling classes and re-verified `type-check`, `lint:ci`, `quality:ci` with auth flow code paths preserved | mitigated |
| R-018 | high | codex | Root `npm run lint:ci` gate can be blocked by local AI-agent/playwright helper trees being linted as production code (`.agent/**`, `.agents/**`, `playwright-utils/**`, `tests/**`) | Updated root ESLint global ignores to exclude local tooling/test helper trees from production lint scope; re-ran required checks (`type-check`, frontend/server build+lint, `quality:ci`) and confirmed pass | mitigated |
| R-019 | low | codex | Universal `prefers-reduced-motion` CSS rule with `!important` may suppress intentional micro-animations that provide UI state feedback | All motion suppression is strictly gated behind OS-level user preference; users who request reduced motion explicitly want this behavior per WCAG 2.3.3 | accepted |
| R-020 | low | codex | Color contrast changes (`text-white/30 -> /50`, `text-white/40 -> /50`) alter visual weight of secondary text elements | Changes are minimal opacity shifts preserving design hierarchy; all changes verified against WCAG AA 4.5:1 minimum; no primary UI elements affected | accepted |
| R-021 | medium | codex | BATCH:014 execution docs/evidence can drift from actual gate state (false pass claim) and generated artifacts can pollute commit history | Corrected BATCH:014 checks/evidence text to reflect full required gate set, removed generated artifacts from tracking, and re-verified `quality:ci` before closing | mitigated |
